# Java-001-To-do-list
This Java To-Do List program allows users to manage tasks through a simple console menu. It uses an **ArrayList** to store tasks and a **Scanner** for user input. Users can add, view, and delete tasks, and the program runs continuously until the user chooses to exit.
